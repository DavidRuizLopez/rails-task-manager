<h1>My Task</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, show_path(task) %>
      <%= link_to "Delete", delete_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
    </li>
  <% end %>
</ul>


<p><%= link_to 'add new task', :new %></p>
